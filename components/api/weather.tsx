// components/apis/weather.ts
// 실제 팀의 날씨 API가 있다면 여기 구현을 교체하세요.
export async function getRainMmH(lat: number, lon: number): Promise<number> {
    // TODO: 상대방이 구현한 날씨 API 호출로 교체
    // 예: return await fetchQWeather(lat, lon);
    return 12; // 임시값(테스트용)
  }
  